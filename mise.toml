[tools]
python = "3.13"
uv = 'latest'

[tasks.default]
alias = 'default'
run = 'uv run granian app:app.asgi'

[env]
# Granian configuration
GRANIAN_HOST = '127.0.0.1'
GRANIAN_PORT = 5000
GRANIAN_WORKERS = 2
GRANIAN_THREADS = 1
GRANIAN_INTERFACE = 'asginl'
GRANIAN_RELOAD = false

# Django
ALLOWED_HOSTS = 'localhost 127.0.0.1 [::1]'

_.file = '.env'
